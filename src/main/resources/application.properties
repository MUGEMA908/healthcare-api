# ============================================================
# SMART HEALTHCARE SYSTEM - Application Configuration
# Author: Eugene NIYONSABA | Final Year Project
# ============================================================

# --- Server ---
server.port=${PORT:8080}

# --- PostgreSQL Database (Render - Production) ---
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- PostgreSQL Database (Local - Development) ---
# spring.datasource.url=jdbc:postgresql://localhost:5432/healthcare_db
# spring.datasource.username=postgres
# spring.datasource.password=admin123

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# --- JWT ---
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# --- Logging ---
logging.level.com.healthcare=INFO
logging.level.org.springframework.security=INFO