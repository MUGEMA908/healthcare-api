# ============================================================
# SMART HEALTHCARE SYSTEM - Application Configuration
# Author: Eugene MUGEMA | Final Year Project
# ============================================================

# --- Server ---
server.port=8080

# --- PostgreSQL Database ---
spring.datasource.url=jdbc:postgresql://localhost:5432/healthcare_db
spring.datasource.username=postgres
spring.datasource.password=admin123

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# --- JWT ---
jwt.secret=SmartHealthcare2024SecretKeyForJWTTokenGeneration123456789
jwt.expiration=86400000

# --- Logging ---
logging.level.com.healthcare=DEBUG
logging.level.org.springframework.security=INFO
